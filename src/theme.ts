import { extendTheme } from "@chakra-ui/react";

export const colors = {
  fb: "#0386FF",
  cloud: "#EEEEEE",
  clouder: "#DDDDDD",
  block: "#F4F4F4"
};

const theme = extendTheme({ colors });

export default theme